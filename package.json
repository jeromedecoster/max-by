{
  "name": "max-by",
  "version": "0.0.0",
  "main": "index.js",
  "devDependencies": {
    "tape": "~3.0",
    "zuul": "1"
  },
  "scripts": {
    "old": "zuul -- test/*.js",
    "yo": "echo $SAUCE_LABS",
    "test": "[[ $SAUCE_LABS == true ]] && zuul -- test/*.js || tape test/*.js"
  }
}

{
  "name": "max-by",
  "version": "0.0.0",
  "main": "index.js",
  "devDependencies": {
    "tape": "~3.0",
    "zuul": "1"
  },
  "scripts": {
    "old": "zuul -- test/*.js",
    "test": "[ -z \"$SAUCE_LABS\" ] && echo oui || echo non",
    "tmp": "[[ $SAUCE_LABS == true ]] && zuul -- test/*.js || tape test/*.js"
  }
}

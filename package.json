{
  "name": "max-by",
  "version": "0.0.0",
  "main": "index.js",
  "devDependencies": {
    "tape": "~3.0",
    "zuul": "1"
  },
  "scripts": {
    "old": "zuul --local -- test/*.js",
    "test": "[ -z \"$SAUCE_LABS\" ] && tape test/*.js || zuul --local -- test/*.js",
    "tmp": "[[ $SAUCE_LABS == true ]] && zuul -- test/*.js || tape test/*.js"
  }
}

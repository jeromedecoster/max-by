{
  "name": "max-by",
  "version": "0.0.0",
  "main": "index.js",
  "devDependencies": {
    "tape": "~3.0",
    "zuul": "1"
  },
  "scripts": {
    "old": "zuul -- test/*.js",
    "test": "[ \"x$(BROWSER_NAME)\" = 'x' ] && tape test/*.js || zuul -- test/*.js"
  }
}
